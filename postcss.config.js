/** @type {import('postcss').Config} */
module.exports = {
  plugins: {
    // Usa Tailwind CSS como plugin de PostCSS
    tailwindcss: {},
    // Autoprefixer para agregar prefijos CSS automáticamente
    autoprefixer: {},
  },
};
